# CreatureGRC Environment Variables - Minimal
# Connections to existing Proxmox LXC services only

# ============================================================================
# GRC Core Database (local to GRC Core LXC)
# ============================================================================
GRC_DB_NAME=grc_platform
GRC_DB_USER=grc_user
GRC_DB_PASSWORD=changeme_secure_password
GRC_DB_PORT=5432

# ============================================================================
# Existing LXC Services (via IP/API)
# ============================================================================

# Wazuh (LXC - Proxmox helper script installed)
WAZUH_API_URL=https://192.168.1.10:55000
WAZUH_USER=grc-collector
WAZUH_PASSWORD=changeme

# Netbox (LXC - Proxmox helper script installed)
NETBOX_API_URL=http://192.168.1.11:8000
NETBOX_TOKEN=changeme

# Keycloak (LXC - Proxmox helper script installed)
KEYCLOAK_URL=http://192.168.1.20:8080
KEYCLOAK_REALM=master
KEYCLOAK_CLIENT_ID=creaturegrc
KEYCLOAK_CLIENT_SECRET=changeme

# FreeIPA (LXC - Proxmox helper script installed)
FREEIPA_API_URL=https://192.168.1.21
FREEIPA_USER=grc-collector
FREEIPA_PASSWORD=changeme

# Infisical (LXC - Proxmox helper script installed)
INFISICAL_API_URL=http://192.168.1.22:8080
INFISICAL_TOKEN=changeme

# Vaultwarden (LXC - Proxmox helper script installed)
VAULTWARDEN_API_URL=http://192.168.1.23:8080
VAULTWARDEN_TOKEN=changeme

# OneDev (LXC - Proxmox helper script installed)
ONEDEV_API_URL=http://192.168.1.30:6610
ONEDEV_TOKEN=changeme

# Zot Registry (LXC - Proxmox helper script installed)
ZOT_API_URL=http://192.168.1.31:5000
ZOT_USER=grc-collector
ZOT_PASSWORD=changeme

# ============================================================================
# AI Foundry (Separate LXC - docker-compose.ai-foundry.yml)
# ============================================================================
LITELLM_API_URL=http://192.168.1.101:4000
LITELLM_MASTER_KEY=changeme

TEMPORAL_HOST=192.168.1.101:7233
TEMPORAL_DB_PASSWORD=changeme

OBOT_API_URL=http://192.168.1.101:9000
OBOT_SECRET_KEY=changeme
OBOT_DB_PASSWORD=changeme

GOOSEAI_DB_PASSWORD=changeme

LANGFUSE_API_URL=http://192.168.1.101:3001
LANGFUSE_PUBLIC_KEY=pk-lf-changeme
LANGFUSE_SECRET_KEY=sk-lf-changeme
LANGFUSE_DB_PASSWORD=changeme
LANGFUSE_NEXTAUTH_SECRET=changeme
LANGFUSE_SALT=changeme
LANGFUSE_PROJECT_NAME=CreatureGRC

REDIS_PASSWORD=changeme

# ============================================================================
# LLM API Keys (for LiteLLM in AI Foundry)
# ============================================================================
ANTHROPIC_API_KEY=sk-ant-changeme
OPENAI_API_KEY=sk-changeme
GOOGLE_API_KEY=changeme
AZURE_API_KEY=changeme

# ============================================================================
# Cloud/SaaS Platforms (Shadow IT Discovery)
# ============================================================================

# Vercel - Hosting and deployments
VERCEL_API_TOKEN=changeme
VERCEL_TEAM_ID=  # Optional - leave blank for personal account

# Supabase - Backend-as-a-service
SUPABASE_ACCESS_TOKEN=changeme

# Neon - Serverless PostgreSQL
NEON_API_KEY=changeme

# GitHub - Code repositories and actions
GITHUB_TOKEN=ghp_changeme
GITHUB_ORG=yokoszn

# Cloudflare - DNS, CDN, and security
CLOUDFLARE_API_TOKEN=changeme
CLOUDFLARE_ACCOUNT_ID=changeme

# v0.dev tracking (via Vercel API - no separate token needed)
# v0.dev projects are detected automatically through Vercel integration

# ============================================================================
# Ticketing & Email (External Services)
# ============================================================================

# Jira/JSM (Cloud)
JIRA_URL=https://yourorg.atlassian.net
JIRA_EMAIL=grc@example.com
JIRA_API_TOKEN=changeme

# M365 Email (SMTP)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=grc@example.com
SMTP_PASSWORD=changeme

# Slack/Teams Webhooks (for alerts)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/changeme
TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/changeme
